export type PreloadScreenCount = [number, number];
export type PaddingOption = number | string;

export interface SpaceOption {
  index: number;
  item: unknown;
  column: number;
  top: number;
  left: number;
  bottom: number;
  height: number;
}
